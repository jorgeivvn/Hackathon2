<!DOCTYPE html>
<html>
  <head>
    <title>Simple Map</title>
    <meta name="viewport" content="initial-scale=1.0">
    <meta charset="utf-8">
    <link href="https://fonts.googleapis.com/css?family=Roboto" rel="stylesheet">
    <link href="https://use.fontawesome.com/releases/v5.0.6/css/all.css" rel="stylesheet">

    <style>
      /* Always set the map height explicitly to define the size of the div
       * element that contains the map. */
      #map {
        height: 130%;
        z-index: 0;
        position: relative;
        bottom: 555px;
        left: 200px;

      }
      /* Optional: Makes the sample page fill the window. */
      html, body {
        height: 100%;
        margin: 0;
        padding: 0;
        font-family: 'Roboto', sans-serif;
        background-color: #f4a142;
      }
      .sidebar {
      flex-direction: column;
      margin-bottom: 40px;
      margin-left: 20px;
      }
      button {
        width: 25px;
        height: 20px;
        margin: 10px;
      }
      #plus {
        margin: 10px;
      }

    </style>
  </head>
  <body>
    <div class='sidebar'>
    <h1>AnnoMap</h1>
      <div class='user'>
        <img src=''>
        <p class='userName'>Julia Brooks</p>
        <p class='userEmail'>Jbrooks@gmail.com</p>
      </div>
      <div class='groupsList'>
        <h2 class='groupsTitle'>Groups:</h2>
        <button id="friends"><i class="fa fa-times"></i></button><span> Friends</span><br>
        <button id="family"> </button><span> Family</span><br>
        <button id="parks"> </button><span> Parks</span><br>
        <button id="food"> </button><span> Food Trucks</span><br>
        <button id="dog"> </button><span> Dog Parks</span><br>
        <button id="bars"></button><span> Bars</span><br>
        <i id="plus" class="fa fa-plus"></i><span> Create new Group</span>
      </div>
    </div>
      <div id="map">
        <img id="popup" src="/images/popup.jpg"/>
      </div>
      <script>
        var map;
        function initMap() {
          map = new google.maps.Map(document.getElementById('map'), {
            center: {lat: 30.307182, lng: -97.755996},
            zoom: 8
          });
        }
      </script>
      <script src='https://maps.googleapis.com/maps/api/js?key=<%=googleAPI%>&callback=initMap'
      async defer></script>
  </body>
</html>
